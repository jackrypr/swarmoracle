# Database
DATABASE_URL="postgresql://username:password@localhost:5432/swarmoracle?schema=public"
DIRECT_URL="postgresql://username:password@localhost:5432/swarmoracle?schema=public"

# Redis (for caching and rate limiting)
REDIS_URL="redis://localhost:6379"

# JWT Authentication
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_EXPIRES_IN="24h"

# Server Configuration
PORT=3000
NODE_ENV=development

# API Keys (if needed for external services)
# OPENAI_API_KEY=""