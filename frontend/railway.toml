[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx serve -s dist -l $PORT --cors"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
NODE_ENV = "production"

[deploy.healthcheck]
timeout = 100
interval = 15
threshold = 3